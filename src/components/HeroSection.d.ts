interface HeroSectionProps {
    subtitle?: string;
    currentStep?: number;
    totalSteps?: number;
}
declare function HeroSection({ subtitle, currentStep, totalSteps }: HeroSectionProps): import("react/jsx-runtime").JSX.Element;
export default HeroSection;
